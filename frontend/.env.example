# Optional: full API base if you do NOT use the Vite dev proxy (e.g. production or custom port).
# Default in dev: leave unset → uses "/api/market" (proxied to http://localhost:5106)
# VITE_API_BASE_URL=http://localhost:5106/api/market
